# 认知状态驱动的知识卡片生成项目改进计划

## 项目背景与改进目标

### 原有问题

知识卡片仅按内容直接、随机生成，未与用户认知状态、认知进度绑定。

### 改进目标

将知识卡片生成、展示、交互全流程，与用户认知进度绑定，同时也用于更新对于用户认知进度的估计。

 

### 总体改进框架

1. 生成逻辑：结合用户通过界面操作表达的意图（注意力/兴趣）+ 知识状态生成卡片，支持自动触发 + 手动触发。
2. 卡片形态：单卡片提供 3 种视图（查阅视图、问题视图、答题视图），对应 2 种反馈方式（懂/不懂）。
3. 状态更新：根据用户交互结果，更新知识卡片对应知识点的知识状态估计。
4. 状态传播：基于知识关联图，对知识点状态变化进行关联传播。

 

## 知识卡片形态调整

相比较原来的知识卡片，现在的知识卡片在包含内容上略有调整，主要是除了title要多一个不限长度的question，因为原先的title有些太冷冰冰，同时由于使用视图来规范UI并增加了反馈机制，界面样式上也要有调整。

### 卡片组成元素

1. 缩略标题
2. 自测问题：非专业化、高表达性，用于用户快速自测是否理解知识点
3. 内容（Content）：保留懒加载（lazy 生成）
4. 作答交互模块：复用原有作答实现
5. 卡片交互状态：懂/不懂

### 三种视图

我们根据用户的认知需求设置了三种视图，在每种视图中要支持和另外两种视图的切换，同时要支持用户表达懂或不懂，因此需要一些UI上的调整

1. 查阅视图

直接展示内容 Content

2. 自测视图

只展示自测问题

3. 答题视图

允许用户针对question作答，并且LLM判定是否正确予以反馈（可以复用原来的答题功能实现）

### 两种交互反馈

用简洁清晰的方式允许用户表达懂了/存疑

 

## 知识卡片生成触发机制

### 与旧方案区别

取消「仅在首次展开步骤时生成」的逻辑，我们现在把知识卡片认为是对于用户可能需要scaffolding的地方的推荐。这个推荐的触发可以来自于用户一些显式的界面操作反映的意图，这会被映射到我们初始化时生成好的知识Node上，知识卡片就将从这些node中产生。

### 事件触发依据与Knowledge Node对应

通过用户界面交互事件推断意图，我们把主要的意图分为以下几类：

1. 任务分解：在语义层面理解这个步骤的作用，为什么作为一个子步骤。
2. 前置知识：为了理解这个步骤/这段代码，所需要的知识补充
3. 代码理解：如果逻辑复杂或者代码阅读有难度时，帮助理解代码本身
4. 功能映射：理解一个代码的功能在语义层面对应的意图

我们通过事件触发机制（可能需要一个事件维护的数据结构，因为涉及到前序的事件）来推断用户有这些意图，并且按照既定规则retrieve用户可能需要scaffolding的nodes，作为生成这次推荐知识的source。以下是对应表，按照优先级顺序排列：

1. 直接展开一个step：step绑定的背景知识->step在任务分解视角的作用->step的意图如何映射到代码上
2. 此前发生由highlevelstep关联到step或反向的关联：step在任务分解视角的作用->step绑定的背景知识->step的意图如何映射到代码上
3. 由step关联code：step的意图如何设映射到代码上->代码理解
4. 由code关联step：代码的意思在现实中的含义->代码理解

此外，我们对于提问的支持现在也变成只有一个按钮了（保留在extension顶端的提问按钮和弹出），但提问的解析逻辑为首先分析用户的意图包含（任务分解、前置知识、代码理解、功能映射）的哪一个或哪些，同时分析这个问题最为对应的步骤是哪个，然后再根据这些点和其它意图处理一样进入接下来的生成知识卡片逻辑。

此外，还有一类特殊的事件，是用户表现出一些求知以外的回顾或者self-check的动机（也就是“之前行动展现过他们已经有所掌握的意思，现在又来查看”），这时候我们首先默认他们是来自测的，直接呈现自测view。这些事件包括：

1. 二次查看步骤：当再次查看之前展开过的步骤时
2. 乱序查看：之前用户主动直接查看了后续的步骤，反过来查看前面的步骤
3. 用户指定生成更多知识卡片（现在各step还没有这个按钮，需要加上，或者可以把原来的对步骤提问按钮变成添加知识卡片按钮）：说明他们对这个步骤目前的知识卡片基本掌握，但是还想看看自己还有啥没懂的。

在这类事件下，我们默认用户感兴趣的范围是和这个步骤直接关联的所有内容，我们步进到生成知识卡片的逻辑。

## 知识卡片内容生成逻辑

根据知识卡片生成触发机制获取到意图的时候，我们结合着用户的node mastery来构建这次给他们推荐的指示卡片。

1. 获取用户在这个步骤下相关的所有Node的mastery level
2. 提供意图掌握内容和已掌握内容给LLM，要求其组合出尽可能少但是能cover住用户未掌握的、且感兴趣的node的知识卡片（越少越好）
3. 生成知识卡片，要求指定出这个知识卡片对应的node是哪几个（同时分开记录之前推测已经比较掌握的相关node，方便接下来lazy生成），然后生成title和question。
4. lazy生成时保持简短，提供scaffolding，支持用户从当前认知状态进阶到next level。

 

## 知识状态更新

用户和一个知识卡片的互动会产生以下的互动结果：

1. 选择了懂/不懂
2. 没选择，但是在某种view下进行了主要的交互
3. 选择了答题mode，GPT返回了一个掌握程度估计
4. 没查看知识卡片

请你根据这些清晰或模糊的掌握程度表达，用固定常数对应用户每个结果对应的该知识卡片掌握的情况，然后更新这个知识卡片对应的核心node的掌握情况。

## 知识状态关联图传播

在更新了一个点的知识状态之后，需要根据CodeAwareCognitiveEdge的边来传播这个知识状态的估计。我们需要根据一个行为代表着X%掌握了A知识点，(1-X)%没有掌握知识点，再根据conditionalMasteryProbability这个p值（简化处理，如果不会A,那有1-p的概率会B）沿着知识结构去传播给相邻的B知识点，计算方式按照BKT的理论指导即可。一开始传播只传一跳即可，如果一个点同时被两个知识点传播到，则取最大值，不回传到被直接更新的知识点。

请把涉及到知识状态追踪的内容整合到一个模块中，这样方便后续迭代参数与算法。
